#!/usr/bin/env bash
set -e # halt script on error

bundle exec jekyll build

datetimestamp=$(date --rfc-3339=seconds | sed 's/ /T/'| sed -e "s/:/./g"
)
git checkout -b travis_build_$datetimestamp
git add . 
git commit -m "latest jekyll build from travis $datetimestamp"
git push -f "https://github.com/BlockchainInstituteChi/weteachblockchainsite" travis_build_$datetimestamp

# bundle exec htmlproofer ./_site --disable-external
# 445189ffc9dc9f436f7e5e748b0095ab7a24c6ca

# git push -f "https://445189ffc9dc9f436f7e5e748b0095ab7a24c6ca@github.com/BlockchainInstituteChi/weteachblockchainsite" test
